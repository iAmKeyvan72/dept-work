To run the project open the terminal and go to prefered directory and run the following code:
```
git clone git@github.com:iAmKeyvan72/dept-work.git
cd dept-work
yarn install
yarn server
yarn start
```
