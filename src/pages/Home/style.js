import styled from 'styled-components';

export const StyledLoadingHomePage = styled.div`
  position: fixed;
  inset: 0px;
  background: var(--text-900);
  z-index: 99;
  display: flex;
  justify-content: center;
  align-items: center;
`;
